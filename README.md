# Lab storage for scripts

## Upload any scripts used for image processing and bioinformatics

You can upload a file to each subfolder, or create a new folder by adding "/" right after the current folder path.  
E.g. [ (Original path) /(new folder name)]                 
Typing / will automatically create a folder.
